@actor
@space 002
# Q: { "request": "show files", context: "@-workspace", "format": "text, one per line" }
# Q: what files are in the @-workspace? show them one per line.
Q: show files in workspace. Reply with JSON.
Q: does the file x1.c exist in the workspace?
Q: what files are available in the workspace that don't start with "fib"? Reply with JSON.